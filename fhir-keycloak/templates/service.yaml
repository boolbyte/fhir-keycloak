apiVersion: v1
kind: Service
metadata:
  name: {{ include "fhir-keycloak.fullname" . }}
  labels:
    {{- include "fhir-keycloak.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fhir-keycloak.selectorLabels" . | nindent 4 }}
